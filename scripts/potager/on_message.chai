print("Message received from topic " + message.topic +" : "+message.content);

if(message.topic == "home/cuve")
{
    precMessageReceived += 1;

    print(precMessageReceived);

    if(precMessageReceived>4)
    {
        mosquitto.disconnect();
        mosquitto.continue=false;
    }
}
else
{
    print("Message from sensor : " + message.content);
}